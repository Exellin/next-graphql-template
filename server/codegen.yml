overwrite: true
schema: "http://localhost:4000/graphql"
documents: null
generates:
  generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ../client/generated/schema.graphql:
    - print-schema.ts
